# 양산형 SEO 서버(admin FastAPI + worker + runtime) 의존성
fastapi
uvicorn[standard]
jinja2
python-multipart

# indexing 잡: 서비스계정 RS256 JWT 서명
cryptography

# (선택) 분석 스크립트 dedup_cluster — 런타임 워커의 dedup 은 stdlib 라 불필요
# datasketch
# openpyxl
# scikit-learn
